I need you to run a full AKATSUKI model training pipeline on this L40S GPU.

## Steps to execute:

### 1. Open Terminal
Open the Terminal tab in this Studio.

### 2. Clone the repository
```bash
git clone https://github.com/kwkkd/akatsuki-hermes-integration akatsuki
cd akatsuki
```

### 3. Install dependencies
```bash
pip install -q torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
pip install -q transformers accelerate bitsandbytes peft trl datasets scipy pyyaml
```

### 4. Generate security domain corpus
```bash
python corpus_builder.py
```

### 5. Continual Pre-training (QLoRA)
```bash
python continue_pretrain.py --model_id "deepseek-ai/DeepSeek-R1-Distill-Qwen-7B" --qlora --batch_size 2 --lr 5e-5 --epochs 1
```
This takes ~2 hours. It fine-tunes DeepSeek-R1-Distill-Qwen-7B on security domain text using QLoRA.

### 6. SFT (AKATSUKI persona training)
```bash
python train.py --model_id ./pretrained_akatsuki
```
This takes ~40 minutes. It teaches the model the AKATSUKI red-team response style.

### 7. Package the model
```bash
tar czf akatsuki_model.tar.gz merged_hacker_ai_model/
```

### 8. Tell me when complete
Tell me "AKATSUKI model training complete. Download akatsuki_model.tar.gz from the Files tab."

## Notes:
- The model is deepseek-ai/DeepSeek-R1-Distill-Qwen-7B (7B parameters, distilled from DeepSeek-R1)
- Training uses 4-bit QLoRA to fit in GPU memory
- Total expected time: ~3 hours (within the 5 free hours)
- If any step fails, show me the error message and stop
